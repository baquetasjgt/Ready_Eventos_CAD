# Copy to .env and add your Anthropic API key to enable the AI assistant.
# Leaving this unset keeps the app working with the assistant disabled.
# NOTE: a browser-exposed key is visible to end users — move the Claude call
# behind a server proxy before deploying to production.
VITE_ANTHROPIC_API_KEY=
