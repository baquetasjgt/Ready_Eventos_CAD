# Copy to .env and add your Google Gemini API key to enable AI text generation.
# Leaving this unset keeps the app working with the assistant disabled.
# NOTE: a browser-exposed key is visible to end users — move the Gemini call
# behind a server proxy before deploying to production.
VITE_GEMINI_API_KEY=
