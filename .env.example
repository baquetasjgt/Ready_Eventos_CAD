# Copia este fichero a .env y rellena los valores.

# --- Supabase (base de datos + auth + storage) ---
# La URL y la clave "publishable" son públicas por diseño (van en el navegador);
# la seguridad la da el Row Level Security. Se pueden poner tal cual.
VITE_SUPABASE_URL=https://jvjhqdwlhaggoqsnenfw.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_f0DmKuM5XGy5YXikIgoLNw_bNjJjPPb

# --- IA (Gemini) ---
# En local se puede llamar directamente con esta clave. En producción, dejar
# VITE_GEMINI_API_KEY vacío y usar VITE_AI_ENDPOINT (proxy servidor) para que la
# clave NO viaje al navegador.
VITE_GEMINI_API_KEY=
# URL del proxy de IA (Supabase Edge Function / Cloudflare Worker). Si se define,
# la app llama aquí en lugar de a Google directamente.
VITE_AI_ENDPOINT=
